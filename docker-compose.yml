version: '3.9'

services:
  db:
    image: postgres:15
    container_name: skillsync-db
    environment:
      POSTGRES_USER: skillsync
      POSTGRES_PASSWORD: skillsync
      POSTGRES_DB: skillsync
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U skillsync"]
      interval: 5s
      timeout: 5s
      retries: 5

  # NEW SERVICE: ChromaDB (Vector Database)
  chromadb:
    image: chromadb/chroma:latest
    container_name: skillsync-chroma
    ports:
      - "8001:8000"  # Expose on host port 8001 to avoid conflict with backend
    volumes:
      - chroma_data:/chroma/chroma  # Persistent storage
    environment:
      - IS_PERSISTENT=TRUE
      - ANONYMIZED_TELEMETRY=False

  neo4j:
    image: neo4j:latest
    container_name: skillsync-neo4j
    ports:
      - "7474:7474"  # Browser UI
      - "7687:7687"  # Bolt protocol
    environment:
      - NEO4J_AUTH=neo4j/skillsync123
    volumes:
      - neo4jdata:/data

  backend:
    build: ./backend
    container_name: skillsync-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    environment:
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=skillsync123
    depends_on:
      db:
        condition: service_healthy
      chromadb:  # Backend must wait for Chroma
        condition: service_started
    volumes:
      - ./backend:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  postgres_data:
  chroma_data:  # Define the new volume
  neo4jdata:
